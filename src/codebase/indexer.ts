// indexer creates searchable index of codebase files
// todo implement vector embeddings or text indexing
export class Indexer {
  constructor() {
    // setup for file indexing system
  }

  // takes list of files and creates searchable index
  async indexFiles(files: string[]): Promise<void> {
    // todo process files and build index
  }
}

export default Indexer;